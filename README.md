## Usage
```
$ java -jar sqlite2xls.jar
usage: sqlite2xls database... [options]
 -h,--help             display this information and exit
 -p,--ptable <TABLE>   primary tables, each of them recursively left joins
                       its reference tables
```
