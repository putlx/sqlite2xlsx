## Usage
```
$ sqlite2xlsx
usage: sqlite2xlsx database... [options]
 -h, --help   display this information and exit
 -p <TABLE>   primary tables, each of them recursively left joins its reference tables
```
